"""
First get two pointers
first pointer travel until nth node so for example we have 10 elements and we need 4th elemennt from last
then travel first pointer to 4th element
now start the second pointer at 0 and first pointer 4. so when first pointer reaches end the second pointer reaches 6th value which is our value
"""